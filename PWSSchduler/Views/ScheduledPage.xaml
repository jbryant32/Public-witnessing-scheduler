<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models ="clr-namespace:PWSSchduler.Model"
             xmlns:customControls="clr-namespace:PWSSchduler.CustomControls"
             xmlns:viewModels ="clr-namespace:PWSSchduler.ViewModels"
             x:Class="PWSSchduler.Views.ScheduledPage">
    <ContentPage.BindingContext>
        <viewModels:ScheduledViewModel x:Name="ViewModel"/>
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <ListView  BackgroundColor="White" HasUnevenRows="True" ItemsSource="{Binding Bookings}" SelectionMode="None">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <customControls:BookingView Email="{Binding Email}" LocationName="{Binding LocationName}" ScheduledStartTime="{Binding ScheduledStartTime}"/>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </ContentPage.Content>
</ContentPage>